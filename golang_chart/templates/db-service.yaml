apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: {{ .Values.dbnamespace}}
  labels:
    {{- toYaml .Values.labels | indent 4}}
spec:
  selector: 
    {{- toYaml .Values.dblabels | indent 4}}
  clusterIP: None
  ports:
    - port: 3306
